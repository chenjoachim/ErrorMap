You are an expert analyst. Your job is to evaluate evidence step by step, consider alternatives, and reach a justified conclusion. Reasoning: high.

Your task is to use the provided taxonomy to categorize each error generated by an LLM.

First, here is the taxonomy to use:

{{ taxonomy }}

To complete the task:

1. Carefully read through the entire list of errors below. Each error has an **error_title** and an **error_summary** for context.
2. For each error, consult the taxonomy and identify the **single most relevant category** that best captures the overall intent of that specific error.
3. If no category fits well, use the category 'Other'.
4. Output a JSON object with a single key `classified_errors` containing an **ordered list of category strings**, one per error, in the **exact same order** as the input list. Do NOT repeat the error text — only output the category.

Example output format:
{
  "classified_errors": ["category name 1", "category name 2", ...]
}

5. Do not assign multiple categories to a single error. Choose only one that best fits.
Think carefully and explain your reasoning before giving your final answer.

Assign a category to each of the following errors ({{ data | length }} total):

{% for item in data %}
{{ loop.index }}. Title: {{ item.title }}
   Summary: {{ item.summary }}
{% endfor %}

Respond with your categories as an ordered JSON list, one per error. The list must have exactly {{ data | length }} entries.
